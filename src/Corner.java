
public class Corner {
    float longMin;
    float	longMax;
    float	latMin;
    float latMax;

    Corner(float longMin, float longMax, float latMin, float latMax) {
	this.longMin = longMin;
	this.longMax = longMax;
	this.latMin = latMin;
	this.latMax = latMax;
    }
}
